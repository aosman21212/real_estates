<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Unit List View -->
    <record id="view_real_estate_unit_list" model="ir.ui.view">
        <field name="name">real.estate.unit.list</field>
        <field name="model">real.estate.unit</field>
        <field name="arch" type="xml">
            <list string="Units">
                <field name="name"/>
                <field name="building_id"/>
                <field name="floor"/>
                <field name="area"/>
                <field name="bedrooms"/>
                <field name="rent_amount"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="state" widget="badge"
                    decoration-success="state == 'available'"
                    decoration-warning="state == 'maintenance'"
                    decoration-info="state == 'occupied'"/>
                <field name="tenant_id"/>
            </list>
        </field>
    </record>

    <!-- Unit Form View -->
    <record id="view_real_estate_unit_form" model="ir.ui.view">
        <field name="name">real.estate.unit.form</field>
        <field name="model">real.estate.unit</field>
        <field name="arch" type="xml">
            <form string="Unit">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_maintenance" type="object"
                            class="oe_stat_button" icon="fa-wrench">
                            <field name="maintenance_count" widget="statinfo" string="Maintenance"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Unit Number (e.g. A101)"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Location">
                            <field name="building_id"/>
                            <field name="floor"/>
                            <field name="area"/>
                        </group>
                        <group string="Details">
                            <field name="bedrooms"/>
                            <field name="bathrooms"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="rent_amount"/>
                            <field name="state"/>
                            <field name="tenant_id"/>
                        </group>
                    </group>
                    <group string="Notes">
                        <field name="notes" nolabel="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Unit Kanban View -->
    <record id="view_real_estate_unit_kanban" model="ir.ui.view">
        <field name="name">real.estate.unit.kanban</field>
        <field name="model">real.estate.unit</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" class="o_kanban_small_column">
                <field name="name"/>
                <field name="building_id"/>
                <field name="floor"/>
                <field name="rent_amount"/>
                <field name="currency_id"/>
                <field name="state"/>
                <field name="tenant_id"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click">
                            <strong><field name="name"/></strong>
                            <div>Building: <field name="building_id"/></div>
                            <div>Floor: <field name="floor"/></div>
                            <div>Rent: <field name="rent_amount"/> <field name="currency_id"/></div>
                            <div t-if="record.tenant_id.raw_value">
                                Tenant: <field name="tenant_id"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Unit Search View -->
    <record id="view_real_estate_unit_search" model="ir.ui.view">
        <field name="name">real.estate.unit.search</field>
        <field name="model">real.estate.unit</field>
        <field name="arch" type="xml">
            <search string="Search Units">
                <field name="name"/>
                <field name="building_id"/>
                <field name="tenant_id"/>
                <filter string="Available" name="available" domain="[('state', '=', 'available')]"/>
                <filter string="Occupied" name="occupied" domain="[('state', '=', 'occupied')]"/>
                <filter string="Maintenance" name="maintenance" domain="[('state', '=', 'maintenance')]"/>
                <group>
                    <filter string="Building" name="group_building" context="{'group_by': 'building_id'}"/>
                    <filter string="State" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Unit Action -->
    <record id="action_real_estate_unit" model="ir.actions.act_window">
        <field name="name">Units</field>
        <field name="res_model">real.estate.unit</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_real_estate_unit_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add your first unit!
            </p>
        </field>
    </record>

</odoo>

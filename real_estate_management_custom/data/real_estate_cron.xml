<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_generate_recurring_invoices" model="ir.cron">
        <field name="name">Generate Recurring Rent Invoices</field>
        <field name="model_id" ref="model_real_estate_lease"/>
        <field name="state">code</field>
        <field name="code">model._generate_recurring_invoices()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>

</odoo>
